//////////////////////
//////// CRYSTAL COLLECTOR GAME JAVASCRIPT
////    https://github.com/flyboy85749/unit-4-game
////
////    https://flyboy85749.github.io/unit-4-game/
////
/////////
//////////////////////////////////
//////// *************** VARIABLES ******************///////////
////////////////////////////////////////////////////////////////

let counter = [0,0]; // this will keep track of wins/losses

let score = 0; // score tracker

var points = new Array(100);
for (var i = 0; i < 100; i++) {
    points[i] = i + 1;
}

for (var i = 0; i < points.length; i++) {
    // console.log(points[i]); //This prints the values that you stored in the array
}

// need to store random number in a variable for later use
var rand = randomIntFromInterval(19, 120);

// we'll need to store the values generated by random number, for each crystal
var gar = randomIntFromInterval(1,12);
// console.log(gar);

var eme = randomIntFromInterval(1,12);
// console.log(eme);

var sap = randomIntFromInterval(1,12);
// console.log(sap);

var ame = randomIntFromInterval(1,12);
// console.log(ame);








////////////////// ///////////////////////////////
///////////                         //////////////////
/////////// ********** FUNCTIONS ************** //////////
/////////////////////////////////////////////////////////


function startGame() {
    // hide the crystal values
    $(".value").css({"display": "none"});
    
    $(".scr").append(score);
    var audio = new Audio("assets/sounds/265549__vikuserro__cheap-flash-game-tune.mp3");
    audio.play();
}
startGame();

// rand = random # between 19 - 120
// We can also use this for the point values for the crystals
function randomIntFromInterval(min, max) { // min and max included 
    return Math.floor(Math.random() * (max - min + 1) + min);
}

// Pick a crystal
// This will add crystal values to total score
$("#garnet").click(function(){
    $(".scr").append(clickGarnet());
    
    
  });
  $("#emerald").click(function(){
    $(".scr").append(eme);
    
  });
  $("#sapphire").click(function(){
    $(".scr").append(sap);
    
  });
  $("#amethyst").click(function(){
    $(".scr").append(ame);
    
  });

  // need to add each crystal value to get total score
// score = (gar + eme + sap + ame);
// console.log(score);

function clickGarnet (a,b) {
  return (a + b);

}
console.log(clickGarnet(score, gar));

function winLoss () {
  if (score === rand) {
    alert("You Won!");
    counter[0]++;
  }
  else {
    alert("You Lose");
    counter[1]++;
  }
}






////////////////////////////////////////////////////
////////////                    //////////////////
///// ************** PROCESSES ************ /////
////////////////////////////////////////////////////



// change the HTML here


// adds random number to the display
$(".rnd").append(randomIntFromInterval(19, 120));

// displays starting score value

// these four add random numbers to the screen. hidden at start
$(".gar-value").append(gar + " points");
$(".eme-value").append(eme + " points");
$(".sap-value").append(sap + " points");
$(".ame-value").append(ame + " points");





