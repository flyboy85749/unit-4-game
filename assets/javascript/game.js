//////////////////////
//////// CRYSTAL COLLECTOR GAME JAVASCRIPT
////    https://github.com/flyboy85749/unit-4-game
////
////    https://flyboy85749.github.io/unit-4-game/
////
/////////
//////////////////////////////////
//////// *************** VARIABLES ******************///////////
////////////////////////////////////////////////////////////////

let counter = [0, 0]; // this will keep track of wins/losses

let score = 0; // score tracker

var points = new Array(100);
for (var i = 0; i < 100; i++) {
  points[i] = i + 1;
}

for (var i = 0; i < points.length; i++) {
  // console.log(points[i]); //This prints the values that you stored in the array
}

// need to store random number in a variable for later use
var rand = randomIntFromInterval(19, 120); // global scope

// we'll need to store the values generated by random number, for each crystal
const gar = randomIntFromInterval(1, 12);
const eme = randomIntFromInterval(1, 12);
const sap = randomIntFromInterval(1, 12);
const ame = randomIntFromInterval(1, 12);
// console.log(crystalValue);


// set playerTotal to 0
var playerTotal = 0;






////////////////// ///////////////////////////////
///////////                         //////////////////
/////////// ********** FUNCTIONS ************** //////////
/////////////////////////////////////////////////////////

// rand = random # between 19 - 120
// We can also use this for the point values for the crystals
function randomIntFromInterval(min, max) { // min and max included 
  return Math.floor(Math.random() * (max - min + 1) + min);
}

// resets game
function startGame() {
  // hide the crystal values with css display: none
  $(".value").css({ "display": "none" });

  // generates a random number between 19 and 120
  // reassigns the rand variable that we wrote earlier (line 26)
  rand = randomIntFromInterval(19, 120);

  // displays the random number on the page
  $(".rnd").text(rand);

  // initializes score to 0
  score = 0;

  // displays the initial score of 0 on the pagea
  $(".scr").text(score);

  // initializes the playerTotal to 0
  playerTotal = 0;

  // This starts the awesome game music!
  // var audio = new Audio("assets/sounds/265549__vikuserro__cheap-flash-game-tune.mp3");
  // audio.play();
}
startGame();



// Pick a crystal
// This will add crystal values to total score

function crystalClick() {

  $(".gar").click(function () {
    var audio = new Audio("assets/sounds/345297__scrampunk__itemize.wav");
    audio.play();
    playerTotal = playerTotal + gar;
    $(".scr").text(playerTotal);
    winLoss();
  });
  $(".eme").click(function () {
    var audio = new Audio("assets/sounds/345297__scrampunk__itemize.wav");
    audio.play();
    playerTotal = playerTotal + eme;
    $(".scr").text(playerTotal);
    winLoss();
  });
  $(".sap").click(function () {
    var audio = new Audio("assets/sounds/345297__scrampunk__itemize.wav");
    audio.play();
    playerTotal = playerTotal + sap;
    $(".scr").text(playerTotal);
    winLoss();
  });
  $(".ame").click(function () {
    var audio = new Audio("assets/sounds/345297__scrampunk__itemize.wav");
    audio.play();
    playerTotal = playerTotal + ame;
    $(".scr").text(playerTotal);
    winLoss();
  });
}
crystalClick();







// this function will keep track of wins/losses and player scores
function winLoss() {

  if (playerTotal === rand) {

    alert("You Won! Your score was " + playerTotal);
    // increase win counter by 1
    counter[0]++;
    // display wins on page
    $(".winning").text(counter[0]);
    // reset values in startGame for another round
    startGame();
  }

  else if (playerTotal > rand) {

    // if playerTotal is greater than random number you lose
    alert("You Lose. Your score was " + playerTotal + ". Better luck next time.");


    // increases loss counter by 1
    counter[1]++;
    // displays losses on page
    $(".losing").text(counter[1]);
    // reset values in startGame for another round
    startGame();
  }

}







////////////////////////////////////////////////////
////////////                    //////////////////
///// ************** PROCESSES ************ /////
////////////////////////////////////////////////////



// change the HTML here





